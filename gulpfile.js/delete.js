const { src, task, parallel, series} = require("gulp");
const clean = require("gulp-clean");
const path = require('path');

function getDirectory(path, path.join(__dirname) {

}
    ){

    return src(dir, {read: false})
        .pipe(clean({force: true}))
}
task(delete(dir))

module.exports = {
    delete(dir) : delete
}
